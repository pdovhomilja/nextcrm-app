version: "3"
services:
  my-nextjs-app:
    image: nextcrmio/nextcrm:0.0.3-beta
    container_name: nextcrm-0.0.3-beta
    ports:
      - "3000:3000"
    volumes:
      - .env:/app/.env
      - .env.local:/app/.env.local
